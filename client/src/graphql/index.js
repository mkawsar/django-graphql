import {
    AUTH_LOGIN_MUTATION,
    USER_REGISTRATION_MUTATION
} from './authQuery';
import {
    AUTHORS_QUERY
} from './authorQuery'

export {
    AUTH_LOGIN_MUTATION,
    USER_REGISTRATION_MUTATION,
    AUTHORS_QUERY,
}
